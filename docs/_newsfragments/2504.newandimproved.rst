The codebase's internal type annotations were improved to pass
``mypy --strict falcon/``. To that end, we added a handful of
``# type: ignore``\s for known limitations, but we are working on reducing the
number of these too.
